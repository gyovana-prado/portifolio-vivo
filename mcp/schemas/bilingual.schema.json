{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "bilingual.schema.json",
  "title": "Bilingual string",
  "description": "Every human-readable string is an {en, pt} object. Both required and non-empty; a missing translation fails the build, deliberately.",
  "type": "object",
  "additionalProperties": false,
  "required": ["en", "pt"],
  "properties": {
    "en": { "type": "string", "minLength": 1 },
    "pt": { "type": "string", "minLength": 1 }
  }
}
